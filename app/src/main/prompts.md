# 背景知识：

用户在kotlin普及以前曾经是一个资深安卓开发工程师。

目前主要使用Java语言进行开发，能看懂Kotlin代码的逻辑和含义，但无法亲自下手编写，只能进行一些修改配置、修改变量值、复制粘贴逻辑等程度的简单修改。

当前项目是一个开源的拼音输入法项目，使用Kotlin和C++编写，用户想要定制一个自己的版本用于日常使用。改动的地方仅限于Kotlin的部分，不包含任何C++的部分。

当前打开的文件夹只是完整项目的一小部分，请基于这一小部分进行修改。

用户已经阅读了代码，并试着规划了自己的修改计划。

用户会给出明确的，基于代码逻辑程度指示的内容，请对此在用户指出的文件或片段中进行修改，非必要不修改其它文件，或者尝试运行构建等命令。

用户会自行检查并测试代码的正确性，因此请省略检查部分。另外用户有较强的学习能力，可能会亲自下手修改代码。若读取到的代码和以往不一致，请保留用户修改的内容。

接下来请帮助用户完成修改需求。

# 用户需求

## 需求简介
在用这个输入法之前，我曾经在使用另一个闭源的由商业公司推出的闭源输入法。这个输入法有一个特色功能，即在同一个按键上，上滑、左滑、右滑均可以打出不同的符号，且可以通过皮肤进行配置。
因为偶尔有在手机上编写代码或者运行命令的需求，因此我使用皮肤配置了一套自定义的符号键位。
现在我想要将这个自定义的键位移植到这个新的输入法上。这个输入法只支持单个滑动输入符号的功能。

我已经阅读了项目的代码，并明确了需要修改的内容。

## 修改计划

我已经修改了按键布局和场景切换逻辑，使其大部分接近我此前使用的输入法。

### 总体需求描述
1. 我想要修改现有的QWERTY键盘按键，使其除了上/下滑输入符号以外，还可以通过左滑与右滑输入不同的符号
2. 不同的按键可能会有不同的符号布局。有些按键仅有上/下滑输入的符号，有些按键有向左滑动和向右滑动的符号，并且可以开关是否能够向上滑动并同时输入两个符号

### 代码修改步骤

根据我对代码的解读，目前得出的线索如下：  
我注意到软件包 `org.fcitx.fcitx5.android.input.keyboard` 下存放了许多和键盘与按键有关的代码  
其中：  
QWERTY布局在 TextKeyboard 类中定义  
KeyDef用于存放按键的基本数据和行为  
KeyDefPreset.kt存储了许多不同的按键的类型  
KeyAction 类则存储了许多预定义的动作（）  
KeyView则是按键显示的类（外观或许可以在这里修改）  

根据研究，你可能需要完成以下步骤
1. 在KeyDef的Behavior类里添加SwipeLeft和SwipeRight属性（我已帮你完成）
2. 在BaseKeyboard类中添加对应的手势检测
3. 修改KeyDefPreset，为AlphabetKey添加左右滑动输入字符的行为（我已添加对应字段），并设置合适的popup。另外，你可以简单地将上面符号显示的地方设置成两个符号中间加俩个空格，以模仿靠左靠右显示的行为。  
4. 将其中几个按键作为例子，分别展示「只能输入一个符号」「左滑右滑都能输入符号」「上滑还能同时输入两个符号」的示例。

我已经修改了TextKeyboard以添加示例，并在
因为我不会编写Kotlin，上面的步骤可能会有所遗漏。因此不必强制遵循步骤，实现需求即可。

另外经过前几个AI的失败经历，我注意到一点：
1. 